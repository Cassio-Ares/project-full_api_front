/**
 * organização base das pastas de rota e seus respectivos layouts
 * 
 *  1- auth e visitantes com nome entre parenteses:
 *     * primeiramente serve para  organização;
 *     * segundo ajuda no organização pois podemos colocar um layouts para cada setor(page) ou pages lidados a mesma pasta 
 *     * terceiro serve para criação e organização de rotas.
 * 
 * 
 *   2 - pasta com nome dentro de [ ];
 *     * cria uma rota dinamica que pode mudar dependendo da entrada ou dos dados do usuario;
 *     * o conteudo dentro de [ ] normalmente é a variavel que vai determinar como e pq das mudanças; 
 */